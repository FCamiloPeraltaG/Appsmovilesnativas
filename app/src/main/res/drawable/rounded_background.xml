<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Color de fondo -->
    <solid android:color="@color/white"/>

    <!-- Bordes redondeados -->
    <corners android:radius="16dp"/>

    <!-- (Opcional) Borde con color -->
    <stroke
        android:width="1dp"
        android:color="@color/gray"/>
</shape>